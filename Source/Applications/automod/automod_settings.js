const system = require("../../config.js");
const primary_color = `${system.COLORS.primary}`;

module.exports = {
    data: {
        type: 1,
        name: "settings",
        description: "View current Auto-Mod settings, such as enabled filters and thresholds."
    },
    code: ``
}
