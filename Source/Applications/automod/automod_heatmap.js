const system = require("../../config.js");
const primary_color = `${system.COLORS.primary}`;

module.exports = {
    data: {
        type: 1,
        name: "heatmap",
        description: "Generate a heatmap showing server activity and flagged violations."
    },
    code: ``
}
