const system = require("../../config.js");
const primary_color = `${system.COLORS.primary}`;

module.exports = {
    data: {
        type: 1,
        name: "flagged",
        description: "View all recently flagged messages in the server."
    },
    code: ``
}
