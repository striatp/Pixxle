const system = require("../../config.js");
const primary_color = `${system.COLORS.primary}`;

module.exports = {
    data: {
        type: 1,
        name: "set",
        description: "Configure specific Auto-Mod options."
    },
    code: ``
}
